name: stalzone-artcalc

services:
  artcalc-api:
    image: stalzone-artcalc-api:local
    build:
      context: .
      target: artcalc-api
    expose:
      - "8000"
    environment:
      ALLOWED_HOSTS: "${DOMAIN:?Set DOMAIN in .env},localhost,127.0.0.1,artcalc-api"
      WEB_CONCURRENCY: "${WEB_CONCURRENCY:-2}"
      ARTCALC_TIME_LIMIT: "${ARTCALC_TIME_LIMIT:-0.5}"
      ARTCALC_CP_WORKERS: "${ARTCALC_CP_WORKERS:-1}"
      ARTCALC_NONLINEAR_ITERATIONS: "${ARTCALC_NONLINEAR_ITERATIONS:-3}"
      ARTCALC_FRONTIER_SWEEP_POINTS: "${ARTCALC_FRONTIER_SWEEP_POINTS:-3}"
      ARTCALC_FRONTIER_SOLUTIONS_PER_POINT: "${ARTCALC_FRONTIER_SOLUTIONS_PER_POINT:-2}"
      ARTCALC_MULTI_CONTAINER_SWEEP_POINTS: "${ARTCALC_MULTI_CONTAINER_SWEEP_POINTS:-1}"
      ARTCALC_MULTI_CONTAINER_SOLUTIONS_PER_POINT: "${ARTCALC_MULTI_CONTAINER_SOLUTIONS_PER_POINT:-1}"
      ARTCALC_QUERY_CACHE_SIZE: "${ARTCALC_QUERY_CACHE_SIZE:-64}"
      ARTCALC_FEEDBACK_DB: /app/data/feedback/feedback.sqlite3
      ARTCALC_CONCURRENT_SEARCHES: "${ARTCALC_CONCURRENT_SEARCHES:-1}"
      ARTCALC_UPGRADE_TIME_LIMIT: "${ARTCALC_UPGRADE_TIME_LIMIT:-0.25}"
      ARTCALC_UPGRADE_NONLINEAR_ITERATIONS: "${ARTCALC_UPGRADE_NONLINEAR_ITERATIONS:-2}"
    init: true
    volumes:
      - feedback_data:/app/data/feedback
    read_only: true
    tmpfs:
      - /tmp:size=256m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    logging: &default-logging
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  simulator:
    image: stalzone-simulator:local
    build:
      context: .
      dockerfile: services/simulator/Dockerfile
    expose:
      - "8010"
    init: true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8010/api/simulator/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
    logging: *default-logging

  gateway:
    image: stalzone-gateway:local
    build:
      context: .
      target: gateway
    environment:
      SITE_ADDRESS: "${SITE_ADDRESS:-${DOMAIN:?Set DOMAIN in .env}}"
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      artcalc-api:
        condition: service_healthy
      simulator:
        condition: service_healthy
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    logging: *default-logging

volumes:
  feedback_data:
  caddy_data:
  caddy_config:
